.DS_Store
*.app

# CMake (CMakePresets.json puts every preset under build/<preset>/)
/build/
/build-*/
CMakeUserPresets.json

# Agent worktrees, created by tooling and never committed
.claude/worktrees/

# clangd's index, written beside the sources it indexes
/.cache/
